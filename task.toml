author_name = "Dhruvkumar Patel"
author_email = "dhruvpatel2284@gmail.com"
category = "Cybersecurity"
difficulty = "hard"
tags = ["threat-intelligence", "network-security", "intrusion-detection", "vector-correlation", "incident-response"]
difficulty_explanation = "Requires understanding vector-clock merge semantics, partial order incomparability versus equality, and the difference between temporal recency and aggregate severity for priority ordering. Three bugs across two files with authoritative comments defending each incorrect design choice."
solution_explanation = "Fix 1: Add self._threat[self.segment_id] += 1 after the CORRELATE merge loop in threat_accumulator.py. Fix 2: Replace symmetric boundedness (equality) check with incomparability check (neither dominates) in correlation_analyzer.py. Fix 3: Replace temporal recency sort with vector-sum sort in compute_triage_priority."
verification_explanation = "14 tests in 4 tiers: 6 structural (always pass), 3 state-value (need Fix 1), 3 pair-classification (need Fixes 2+3), 2 full-consistency (need all fixes). NOP gives 8 pass / 6 fail, Oracle gives 14 pass / 0 fail."
expert_time_estimate_hours = 2.5

[task]
name = "threat-intel-repair"
description = "Debug a network security threat correlation engine where segments track threat vectors through PROBE, BREACH, and CORRELATE events. The threat accumulator, correlation analyzer, and report writer contain subtle bugs in vector increment logic, isolation predicate semantics, and triage ordering."

[task.difficulty]
level = "hard"
reasoning = "Three bugs span two files with cascading effects. Bug 2 (equality vs incomparability in partial orders) requires mathematical reasoning about vector lattice semantics. Misleading comments defend each incorrect behavior as a sound engineering decision."

[task.runtime]
entrypoint = "python3 /app/runtime/run_correlation.py"
language = "python"
dependencies = "stdlib-only"

[task.validator]
command = "bash tests/test.sh"
description = "14 pytest tests across 4 tiers."

[task.repair]
command = "bash solution/solve.sh"
description = "Fixes bugs and regenerates outputs."

[task.resources]
timeout = 300
cpu = 2
memory = "2GB"
disk = "1GB"
