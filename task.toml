author_name = "Dhruvkumar Patel"
author_email = "dhruvpatel2284@gmail.com"
category = "Scientific Computing & Numerics"
difficulty = "hard"
tags = ["molecular-dynamics", "vector-clocks", "partial-orders", "energy-simulation", "numerical-computing"]
difficulty_explanation = "Requires understanding of vector-based energy tracking, partial order theory (incomparability vs equality), and the distinction between passive knowledge transfer and active energy dissipation. Three interdependent bugs across two files with misleading comments defending the buggy behavior."
solution_explanation = "Fix 1: Add self._energy[self.particle_id] += 1 after the COUPLE merge loop in dissipation_engine.py. Fix 2: Replace equality check (bidirectional leq) with incomparability check (neither dominates) in field_analyzer.py. Fix 3: Replace temporal recency sort with vector-sum sort in compute_shutdown_priority."
verification_explanation = "14 tests in 4 tiers: 6 structural (always pass), 3 state-value (need Fix 1), 3 pair-classification (need Fixes 2+3), 2 full-consistency (need all fixes). NOP gives 8 pass / 6 fail, Oracle gives 14 pass / 0 fail."
expert_time_estimate_hours = 2.5

[task]
name = "particle-energy-repair"
description = "Debug a molecular dynamics energy dissipation simulation where particles track energy vectors through DRIFT, COLLIDE, and COUPLE events. The dissipation engine, field analyzer, and report writer contain subtle bugs in vector increment logic, decoupling predicate, and priority ordering."

[task.difficulty]
level = "hard"
reasoning = "Three bugs span two files with cascading effects. Bug 2 (equality vs incomparability) requires deep understanding of partial order semantics. Misleading comments actively defend incorrect behavior as sound engineering decisions."

[task.runtime]
entrypoint = "python3 /app/runtime/simulate_field.py"
language = "python"
dependencies = "stdlib-only"

[task.validator]
command = "bash tests/test.sh"
description = "Runs 14 pytest tests across 4 difficulty tiers validating energy computation, pair classification, shutdown ordering, and digest integrity."

[task.repair]
command = "bash solution/solve.sh"
description = "Applies fixes to dissipation_engine.py and field_analyzer.py, then regenerates simulation outputs."

[task.resources]
timeout = 300
cpu = 2
memory = "2GB"
disk = "1GB"
