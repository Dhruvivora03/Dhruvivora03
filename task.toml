author_name = "Dhruvkumar Patel"
author_email = "dhruvpatel2284@gmail.com"
category = "Scientific Computing & Numerics"
difficulty = "hard"
tags = ["neuroscience", "eeg-processing", "brain-computer-interface", "spectral-analysis", "vector-math", "partial-orders"]
difficulty_explanation = "Requires understanding vector partial orders in a neuroscience context, distinguishing equality from incomparability in component-wise comparisons, and recognizing that phase-entrainment is an active neural event that generates local field potentials. The misleading comments use neuroscience jargon to justify the buggy passive-only interpretation."
solution_explanation = "Three bugs across two files: (1) spectral_accumulator.py missing self-increment after ENTRAIN merge, (2) coherence_classifier.py checking vector equality instead of incomparability for decoupling, (3) coherence_classifier.py sorting by temporal recency instead of vector sum for priority."
verification_explanation = "14 tests in 4 tiers: 6 structural (always pass), 3 spectral state (need bug 1 fix), 3 coherence classification (need bugs 2+3 fix), 2 consistency (need all fixes). NOP yields 8 pass / 6 fail. Oracle yields 14 pass / 0 fail."
expert_time_estimate_hours = 2.5

[task]
name = "eeg-coherence-repair"
description = "Debug a brain-computer interface pipeline that incorrectly tracks spectral power during phase-entrainment events, misclassifies coherence relationships between electrode channels, and uses wrong priority ordering for feature extraction."

[task.difficulty]
level = "hard"
reasoning = "The bugs involve subtle mathematical distinctions (equality vs incomparability in partial orders) masked by authoritative neuroscience terminology about passive phase-locking vs active cortical discharge. The entrainment self-increment bug requires understanding that phase coupling generates local field potentials, contradicting the convincing comment about passive coherence detection."

[task.runtime]
entrypoint = "python3 /app/runtime/run_bci.py"
language = "python"
dependencies = "stdlib-only"

[task.validator]
command = "bash tests/test.sh"
description = "Runs 14 pytest tests across 4 difficulty tiers validating spectral vectors, coherence classification, priority ordering, and end-to-end digest consistency."

[task.repair]
command = "bash solution/solve.sh"
description = "Fixes spectral self-increment after ENTRAIN, replaces equality with incomparability for channel decoupling, and corrects priority to use vector sum instead of temporal recency."

[task.resources]
timeout = 300
cpu = 2
memory = "2GB"
disk = "1GB"
